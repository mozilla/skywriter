{
    "description": "Ties together the files being edited with the views on screen",
    "dependencies": {
        "DelegateSupport": "0.0",
        "Editor": "0.0",
        "Filesystem": "0.0"
    },
    "provides": [
        {
            "ep": "extensionpoint",
            "description": "Notifies that the current file is changed.",
            "name": "bufferFileChanged"
        }
    ]
}
