{
    "description": "Ties together the files being edited with the views on screen",
    "dependencies": {
        "text_editor": "0.0",
        "filesystem": "0.0"
    },
    "provides": [
        {
            "ep": "factory",
            "name": "session",
            "pointer": "#createSession",
            "action": "call"
        }
    ]
}
