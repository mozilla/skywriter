{
    "description": "Provides the file and directory model used within Bespin",
    "dependencies": {
        "types": "0.0"
    },
    "provides":
    [
        {
            "ep": "type",
            "name": "existingFile",
            "description": "A pointer to a file which we believe to already exist",
            "pointer": "types#existingFile"
        }
    ]
}
